# Lumine Hedge Fund — Batch 2/3 Completion Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Menuntaskan semua task tersisa dari checkpoint-batch2 (deploy EA, deploy frontend, UI/UX fixes, system config, API keys + webhook, LLM routing, Autogen Studio, journal, health page, dashboard) plus EA v4 (fitur major).

**Architecture:** 3 jalur kerja — (A) Deploy & infrastructure ke VPS, (B) Frontend UI/UX fixes, (C) Backend/System config + integrasi. Setiap task berakhir dengan quality gate (test hijau) sebelum commit. Per metode kolaborasi baru: fitur major → branch `feat/<nama>` + PR ke dev; fix minor → langsung ke dev.

**Tech Stack:** FastAPI + SQLAlchemy + Redis (backend), React/Vite/Tailwind/shadcn (frontend), MQL5 (EA), Docker Compose + Caddy (VPS), GitHub Actions (CI/CD auto-deploy).

**Status baseline:** dev = `d0a7996` (CI hijau, PR #4 merged ke main `e507502`), VPS sudah deploy backend fix batch2.

---

## Workstream A — Deploy & Infrastructure (VPS)

### Task A1: Deploy EA v3.11 (SeedRecentM1) ke VPS

**Objective:** Compile + deploy LumineEA.mq5 versi terbaru (SeedRecentM1) ke container lumine-mt5 agar seed bars 1m berjalan.

**Files:**
- `scripts/deploy/mt5/LumineEA.mq5` (source, version 3.10+SeedRecentM1)
- `scripts/compile_ea_v3.sh` (helper compile)

**Steps:**
1. Cek versi EA di repo vs container: `grep -m2 -E "version|VERSION" scripts/deploy/mt5/LumineEA.mq5` vs `docker exec lumine-mt5 grep ... /opt/lumine-ea/LumineEA.mq5`
2. Copy source ke container: `docker cp scripts/deploy/mt5/LumineEA.mq5 lumine-mt5:/opt/lumine-ea/LumineEA.mq5`
3. Restart container: `docker restart lumine-mt5` (entrypoint.sh meng-compile otomatis jika ada — verifikasi)
4. Verifikasi: `docker exec lumine-mt5 ls -la /opt/lumine-ea/*.ex5` + log EA (SeedRecentM1 aktif)

**Verifikasi:** ex5 timestamp baru + log EA menampilkan seeding M1.

### Task A2: Deploy frontend fix (useSSE heartbeat + useNetworkPing)

**Objective:** Frontend terbaru (fix SSE flicker + network ping) live di VPS.

**Steps:**
1. Push ke dev → CI auto-deploy build frontend (atau manual: `docker compose -f docker-compose.vps.yml build --no-cache frontend && up -d --force-recreate frontend`)
2. Verifikasi: curl frontend + cek container healthy.

---

## Workstream B — Frontend UI/UX

### Task B1: Wire useNetworkPing ke TopBar

**Objective:** Tampilkan "NET x ms" + status koneksi di header.

**Files:**
- Modify: `frontend/src/components/streams/stream-status-list.tsx` (atau komponen TopBar yang ada — cek dulu lokasinya)
- Modify: `frontend/src/app/pages/terminal.tsx` atau layout yang render TopBar

**Steps:**
1. Cari komponen TopBar/header: `grep -rn "TopBar\|NET " frontend/src/`
2. Panggil `useNetworkPing()` di komponen header
3. Render latency + jitter (format `NET 24ms ±3ms`, merah jika !ok)
4. Test: `npm test` + `tsc --noEmit` hijau.

### Task B2: Stream health 5/5 REAL jujur

**Objective:** TopBar tidak tampil "0/5" atau "5/5" palsu — pakai label "—"/"connecting" sebelum stream register.

**Files:**
- Modify: komponen stream health di header (TopBar)
- Modify: `frontend/src/stores/streamStore.ts` (kalau perlu expose status awal)

**Steps:**
1. Identifikasi render "x/5" — cek `streamStore` `getAllStreams()` + komponen header
2. Jika belum ada stream ter-register → tampil "—" bukan "0/5"
3. Test hijau.

### Task B3: Tabel positions & orders — pagination + scroll

**Objective:** Tabel tidak kepanjangan; 1 halaman terbatas + scroll overflow/load more.

**Files:**
- Modify: halaman yang render tabel positions/orders (cek `frontend/src/app/pages/` — mungkin `dashboard.tsx` atau komponen `orders/`)
- Modify: `frontend/src/components/ui/data-table.tsx` (kalau perlu tambah pagination)

**Steps:**
1. Identifikasi tabel positions & orders yang panjang
2. Tambah `max-h` + `overflow-y-auto` + limit rows per halaman (mis. 20) + tombol load more / next
3. Test hijau.

### Task B4: Rapikan header web & tablet (TopBar + CommandBar)

**Objective:** Header nyaman dilihat di web dan tablet.

**Files:**
- Modify: komponen header/TopBar + CommandBar
- Test: `frontend/src/app/pages/terminal.test.tsx`

**Steps:**
1. Audit layout header di breakpoint md/lg
2. Fix spacing/overflow/wrap
3. Test hijau.

### Task B5: Tabel committee + quote & risk UI + responsive

**Objective:** Committee table tidak kosong; quote & risk UI lebih cantik; responsive.

**Files:**
- Modify: `frontend/src/app/pages/dashboard.tsx` (committee, quote, risk cards)
- Modify: `frontend/src/components/dashboard/*` (cards)
- Cek: `frontend/src/stores/committeeStore.ts` (data committee)

**Steps:**
1. Cek kenapa committee kosong — API endpoint? store? wiring?
2. Perbaiki data flow committee
3. Polish quote & risk UI (align, typography, spacing)
4. Test hijau.

### Task B6: Dashboard — P&L chart penuh container + no demo data

**Objective:** Chart P&L mengisi container (height fix) + tidak ada data demo — tampilkan "waiting for live data".

**Files:**
- Modify: `frontend/src/app/pages/dashboard.tsx` (ChartCard height 96 → penuh)
- Modify: `frontend/src/components/charts/*` (kalau perlu)
- Cek: `useEquityCurve` / `usePerformanceMetrics` data source

**Steps:**
1. Fix height P&L chart (container penuh)
2. Jika data kosong/demo → hook "waiting for live data" state
3. Test hijau.

### Task B7: LLM routing page UI lebih menarik (node SVG, garis dinamis)

**Objective:** LLM routing visual seperti diskusi antar-LLM & komite — node SVG, garis dinamis.

**Files:**
- Modify: `frontend/src/components/superadmin/llm-routing-tab.tsx`
- Create: `frontend/src/components/superadmin/llm-routing-graph.tsx` (SVG node graph)

**Steps:**
1. Desain node graph (LLM nodes + committee nodes, garis routing)
2. Implementasi SVG dengan animasi garis (Motion)
3. Test hijau.

### Task B8: System/API health page detail

**Objective:** Health page detail kondisi system + AI state; multicurrency stream health.

**Files:**
- Modify: `frontend/src/app/pages/health.tsx`
- Cek: backend `/health` response (cukup detail?)

**Steps:**
1. Audit data yang tersedia dari `/health`
2. Tambah detail AI state (agent status, LLM gateway health) + multicurrency stream health
3. Test hijau.

---

## Workstream C — Backend/System Config

### Task C1: System config ADVANCED — LLM gateway hot/live

**Objective:** Superadmin bisa atur semua LLM gateway live: API key, base URL per provider, model routing per agent; auto-fallback model (deepseek down → cari model ready di 9router).

**Files:**
- Modify: `backend/src/lumine/api/routers/admin.py` (config CRUD endpoints)
- Modify: `frontend/src/app/pages/superadmin.tsx` (ConfigTab)
- Cek: 9router integration (`LLM_GATEWAY_API_KEY` secret, docs/9ROUTER-SETUP.md)

**Steps:**
1. Riset state saat ini: endpoint config apa yang ada? 9router API untuk upstream discovery?
2. Backend: CRUD provider config (api_key, base_url, model per agent) + auto-fallback logic
3. Frontend: UI form config advanced
4. Test backend + frontend hijau.

### Task C2: Halaman API Keys berfungsi penuh + webhook

**Objective:** API keys CRUD penuh + tambah webhook (Hermes agentic chat / WhatsApp) — jika webhook cukup, tidak buat channel baru.

**Files:**
- Modify: `frontend/src/app/pages/admin-keys.tsx`
- Modify: `backend/src/lumine/api/routers/admin.py`
- Referensi: skill `hermes-agent` (webhook Hermes)

**Steps:**
1. Audit API keys endpoint (sudah ada `useApiKeys`, `useCreateApiKey`, `useRevokeApiKey`)
2. Pastikan CRUD penuh berfungsi (test)
3. Tambah webhook config (endpoint + UI) — riset dulu cara terbaik (Hermes webhook)
4. Test hijau.

### Task C3: Journal enhancement — reason LLM, tindak lanjut, RL training data

**Objective:** Journal tidak plain/demo — reason LLM, tindak lanjut, paper trading, training data model.

**Files:**
- Modify: `frontend/src/app/pages/journal.tsx`
- Modify: `backend/src/lumine/api/routers/journal.py`
- Cek: `docs/08-trading/` (journal design)

**Steps:**
1. Audit journal schema/endpoint saat ini
2. Tambah fields: LLM reason, follow-up, paper trading flag
3. UI: render reason LLM + tindak lanjut
4. Test hijau.

### Task C4: Autogen Studio integrasi penuh

**Objective:** Superadmin bisa custom LLM + tambah agent via Autogen Studio; embed UI autogen studio di superadmin (seperti embed noVNC MT5). Riset dulu cara terbaik.

**Files:**
- Modify: `frontend/src/app/pages/superadmin.tsx` (embed iframe/panel)
- Cek: `AUTOGEN-INSTALLATION-GUIDE.md`, container `lumine-autogenstudio`

**Steps:**
1. Riset: Autogen Studio UI port + auth + embed feasibility (noVNC pattern)
2. Implementasi embed + integrasi (custom LLM, agent management)
3. Test.

### Task C5: EA v4 — fitur major (branch feat/ea-v4)

**Objective:** Dual transport (TCP socket + HTTP polling failover), UI panel Lumine di MT5, multicurrency tick collector, magic number scheme, hardening.

**Files:**
- Create branch: `feat/ea-v4` dari dev
- Modify: `scripts/deploy/mt5/LumineEA.mq5`
- Modify: `backend/src/lumine/trading/mt5_bridge.py` (transport protocol)
- Create: `docs/08-trading/ea-v4-design.md` (magic number + multicurrency)

**Steps:**
1. Dokumentasi design (magic number scheme, transport abstraction) — Task C5a
2. Implementasi transport abstraction + failover — Task C5b
3. Multicurrency config + tick collector — Task C5c
4. UI panel MT5 (dark navy + electric blue) — Task C5d
5. PR ke dev (per metode kolaborasi)

---

## Urutan Eksekusi (prioritas)

1. **A1** (EA deploy) — selesaikan segera, seed M1 sudah lama menunggu
2. **A2** (frontend deploy) — fix SSE sudah di repo, tinggal deploy
3. **B1, B2** (TopBar: network ping + stream health jujur) — kecil, cepat, user-visible
4. **B3–B6** (tabel, header, committee, dashboard)
5. **B7, B8** (LLM routing UI, health page)
6. **C1–C4** (system config, API keys+webhook, journal, autogen)
7. **C5** (EA v4 — fitur major, branch sendiri + PR)

## Verification Strategy

- Backend: `cd backend && uv run pytest` (631+ tests) + `uv run ruff check src tests` + `uv run bandit -r src -ll -q`
- Frontend: `cd frontend && npm test` (170 tests) + `node node_modules/typescript/bin/tsc --noEmit` + `npm run build`
- CI: push ke dev → GitHub Actions 4/4 hijau + auto-deploy VPS
- VPS: SSH health check + docker ps + curl /health

## Risks & Open Questions

- **Autogen Studio embed**: port/auth belum diverifikasi — riset dulu, fallback: link out + API integration
- **EA compile di container**: entrypoint.sh harus handle compile — verifikasi mekanisme compile sebelum deploy
- **LLM gateway auto-fallback**: butuh 9router upstream discovery API — cek docs/9ROUTER-SETUP.md
- **Webhook Hermes**: riset cara webhook Hermes agentic chat (skill hermes-agent) sebelum implementasi
- **Committee kosong**: root cause belum pasti (API? store?) — debug dulu di Task B5
