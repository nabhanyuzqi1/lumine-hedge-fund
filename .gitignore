# ── Secrets ────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.sample
credentials.key
*.pem
*.key
# SSH keys (private - NEVER commit these!)
*.rsa
*.dsa
*.ecdsa
*.ed25519
id_rsa*
id_ed25519*
id_ecdsa*
authorized_keys.pub  # Only public keys OK
id_rsa.priv
id_lumine_deploy  # Deploy key for CI/CD automation - NEVER commit this!
*.enc
!*.yml.enc
!scripts/deploy/secrets.env

# ── Python ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.so

# ── Node / Frontend ────────────────────────────────────────────────────────
node_modules/
.next/
dist/
.turbo/
*.tsbuildinfo

# ── IDE & Editor ───────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Docker ─────────────────────────────────────────────────────────────────
docker-compose.override.yml

# ── Data ───────────────────────────────────────────────────────────────────
# Root-anchored: ignore only the top-level runtime artifact dir, NOT source
# directories named `data` (e.g. backend/src/lumine/data/). Per-backend
# artifact dirs are scoped explicitly below.
/data/
backend/.data/
*.db
*.sqlite
*.sqlite3

# ── Logs ───────────────────────────────────────────────────────────────────
*.log
logs/

# ── OS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── Local agent tooling state (not project artifacts) ─────────────────────
.remember/
.openclaude/
.agents/
.claude/
skills-lock.json

# ── Dev scratch state ──────────────────────────────────────────────────────
backend/.data/
backend/.venv/
*.db-journal
# ── Local preview screenshots (never committed) ────────────────
frontend/.shots/

# tooling artifacts
.playwright-mcp/
backend/test_debug.py
