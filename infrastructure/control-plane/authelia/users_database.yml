users:
  admin:
    displayname: "Lumine Admin"
    # argon2id hash — generate with: htpasswd -Argon2id admin
    # replace placeholder below with output, keep quotes
    password: "$argon2id$v=19$m=65536,t=3,p=4$REPLACE_WITH_GENERATED_HASH$"
    email: admin@lumine.local
    groups:
      - admins
    disabled: false
